extends layout

block content
  section.mt3.mb3
    .container
      h1= title
      ol.mxn2.list-reset.flex.flex-wrap.align-stretch
        each member in staff
          li.mb3.px2.col-4.flex.align-stretch
            //- - function colorDice() {
            //-     const classes = ['grab0', 'grab1', 'grab2'];
            //-     function roll(min, max) {
            //-       min = Math.ceil(min);
            //-       max = Math.floor(max);
            //-       return Math.floor(Math.random() * (max - min)) + min; //The maximum is exclusive and the minimum is inclusive
            //-     };
            //-     return classes[roll(0, classes.length)];
            //-   }
            - const colors = ['#0093e9', '#80d0c7', '#cacb41', '#ba0e3a', '#ffe53b', '#ff2525', '#85ffbd', '#fffb7d', '#21d4fd', '#b721ff', '#4158d0', '#c850c0', '#ffcc70', '#ff9a8b', '#ff6a88', '#52acff', '#ffe32c', '#d56bb0', '#ddd6f3', '#b6df35', '#fa8bff', '#2bd2ff', '#2bff88']
            - function randColor(arrOfColors) {return arrOfColors[Math.floor(Math.random() * (arrOfColors.length - 0)) + 0];}
            - function randNum(min, max) {return Math.floor(Math.random() * (max - min)) + min;}
            a.black.hover-no-decoration.colorbar(href=`/staff/${member._id}` style=`background-color: #faaca8; background-image: linear-gradient(${randNum(45, 95)}deg, ${randColor(colors)} 0%, ${randColor(colors)} ${randNum(40, 60)}%, ${randColor(colors)} ${randNum(75, 100)}%)`)
              div.p2.col-12.bg-white.shadow
                figure.mx0
                  img.block.mx-auto.col-7.circle.shadow(src=`/uploads/${member.photo}`, alt="")
                p.mb0.h2.center.dark-blue #{member.fullName}
                p.h4.center.dark-blue #{member.bio}
                ul.list-reset.mb0.pt2.flex.flex-wrap
                  li.flex.flex-center.mr1.mb1.p1.h6.info python
                  li.flex.flex-center.mr1.mb1.p1.h6.info javascript
                  li.flex.flex-center.mr1.mb1.p1.h6.info security
                  li.flex.flex-center.mr1.mb1.p1.h6.info design
                  li.flex.flex-center.mr1.mb1.p1.h6.info people
              //- a(href=`/staff/${member._id}/edit`) Edit
